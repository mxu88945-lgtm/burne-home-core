# ============================================================
#  伯恩主屋版 · 长期记忆系统 —— 环境变量模板
# ------------------------------------------------------------
#  使用方法：复制本文件为 .env.local，再填入你自己的值。
#  .env.local 已被 .gitignore 忽略，绝不会被提交。
#
#  ⚠️ 安全红线：
#   - 真实的 Notion token / GitHub token / 带密钥的 Worker 地址
#     一律只写在 .env.local，绝不写死在代码、绝不提交仓库。
#   - 本文件只放「占位符」，方便别人知道需要哪些变量。
# ============================================================

# Vite 暴露给前端的变量必须以 VITE_ 开头。
# 注意：任何 VITE_ 变量都会被打进前端产物，浏览器里可见。
# 因此真正敏感的 token 推荐走后端 / Worker 代理，不要直接放这里。

# —— Notion 同步（推荐通过自建 Worker 代理，避免 token 暴露在前端）——
# Worker 入口地址（不要在 URL 里带密钥）
VITE_NOTION_PROXY_URL=

# 目标 Notion 数据库 ID（非敏感，可放）
VITE_NOTION_DATABASE_ID=

# —— 多端同步主库（Cloudflare Worker + KV/D1）——
# 主库地址（非密钥，不要在 URL 里带密钥）
VITE_SYNC_WORKER_URL=
# 库标识（区分多人/多库；非敏感）
VITE_SYNC_SPACE_ID=home
# 注意：同步共享密钥 syncKey 属于敏感项，只在 App 设置里填、存浏览器本地，
#       不放这里、不进仓库。

# —— 应用配置 ——
VITE_APP_TITLE=伯恩主屋 · 长期记忆
